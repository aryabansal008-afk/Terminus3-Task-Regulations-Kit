<!--
Terminus 3 instruction.md template.

Guidance (REGULATIONS.md §3):
- Human-written tone, avoid AI-generated writing patterns (no "Let's dive
  in", no excessive bullet-nesting, no restating the obvious).
- Roughly two short paragraphs OR <=20 bullets total. Pick one style, don't
  mix loosely.
- Every requirement needed to succeed must be inferable from what's
  provided here + the environment — don't rely on external/hidden
  knowledge.
- Do not leak oracle logic (don't describe the internal solution approach).
- Use ABSOLUTE filesystem paths for every referenced file, e.g.
  /app/output.json — not "the output file" or "output.json".
- Explicitly state required output file(s) and their exact format.
- No canary strings anywhere in this file.

Delete this comment block before submission.
-->

(Write the task prompt here. Describe the situation/goal, not a step-by-step
checklist — see Strong Task Principle A in reviewer_recovery/ for why:
the agent should reconstruct the specific requirements from evidence in the
environment where possible, not just execute a numbered list.)
